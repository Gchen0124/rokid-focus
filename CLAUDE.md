# Claude / Codex

This is the Rokid Focus daily-priority workspace.

**Read `AGENTS.md` first.** Then `memory/WORKFLOW.md`.

Do not treat this as a generic Android sample. The product is a laptop command center + glasses glance HUD. Memory that must compound lives in `memory/`. Live tasks live in `mock/tasks.json`.
