# Agent queue

One file per delegated task, named `<id>-<slug>.md`.

Empty means the agent has no open handoffs. See `memory/DELEGATION.md`.

Standing daily: `1786973510012-opps-hunting.md` — do not delete after ready; the next agent reopens it.
